/** File for all web server starting up code */
import { startNextJsWebServer } from './web-utils/index.js';

// Re-export the main function for backward compatibility
export { startNextJsWebServer };
